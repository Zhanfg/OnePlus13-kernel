# 2026-07-20 会话续接：从零重编一加13自定义内核

## 状态：Image + AK3 构建成功

### 工作目录（WSL）
- 源码/构建根：`/home/axymorrsen/op13-kernel/`
- 内核树：`/home/axymorrsen/op13-kernel/src/`（官方 OnePlusOSS `oneplus/sm8750_b_16.0.0_oneplus_13`）
- 补丁缓存：`/home/axymorrsen/op13-kernel/patches/`（resukisu/susfs/sched_patch/bbg/re-kernel 等）
- 产物：
  - `Image` 29MB @ `src/arch/arm64/boot/Image`（21:56）
  - `OnePlus13-sun-custom-20260720-2200.zip` 15MB（WSL + 已复制到 `D:\OnePlus13-kernel\`）
  - SHA256: `41aca9cfffd56fa82a1e388d17cb140c06a9963bf53b148016b7d0dfff620a07`

### 已合入并验证（System.map 符号）
| 功能 | CONFIG | 符号数约 |
|------|--------|----------|
| ReSukiSU | CONFIG_KSU=y | ksu_ 240 |
| SuSFS | CONFIG_KSU_SUSFS=y | susfs_ 66 |
| HMBIRD 风驰 | CONFIG_HMBIRD_SCHED=y | hmbird 139 |
| BBRv3 | CONFIG_TCP_CONG_BBR3 + DEFAULT_BBR3 | tcp_bbr3 1 |
| BBG | CONFIG_BBG=y + LSM baseband_guard | bbg_* / __lsm_baseband_guard |
| Re:Kernel | CONFIG_REKERNEL=y | rekernel 7 |
| NTSYNC | CONFIG_NTSYNC=y | ntsync 17 |

### 默认策略（已写入 .config）
- TCP: bbr3
- qdisc: fq
- ZRAM: lz4 (+ writeback/multi_comp)
- KASAN: **关闭**（曾开过导致 Image 54MB，已关掉重编到 29MB）
- KALLSYMS_ALL=y

### 关键修复点（后续勿踩）
1. **HMBIRD 补丁部分合入**：`core.c` 残留 `scx_*` → 改为 `hmbird_*`；补上 `cgroup_read/write_hmbird_deadline`
2. **ReSukiSU 必须 git 布局**：`drivers/kernelsu` → symlink 到 `patches/resukisu/kernel`（父目录有 `.git`）
3. **禁止 git fetch --unshallow**（ReSukiSU/BBG Makefile 会卡网络）— 已注释
4. **BBG**：`drivers/Makefile` 用 `CONFIG_BBG` 不是 `CONFIG_BASEBAND_GUARD`；`CONFIG_LSM` 必须含 `baseband_guard`；完整树含 `tracing/`
5. **Droidspaces** 曾导致 `task_struct` 中 sysvsem/sysvshm 重复声明 — 已删重复

### 辅助脚本（D:\OnePlus13-kernel\scripts\）
- `wsl_fix_and_build.sh` / `wsl_rebuild.sh` / `wsl_status.sh`
- `fix_hmbird_core.py` / `fix_ksu_unshallow.sh` / `fix_bbg_kasan_rebuild.sh`
- `wsl_verify_and_pack.sh`

### 下一步（可选）
1. 真机刷写 AK3 zip（KernelSU/TWRP），跑 `tests/verify_runtime.sh`
2. 确认 wait 默认调度策略与 AK3 音量键菜单
3. 评估是否关闭 KASAN 以外的 debug 选项进一步瘦身
4. 将成功流程固化进仓库 `build.sh` / CI（替换失败的 GitHub Actions stub 方案）
