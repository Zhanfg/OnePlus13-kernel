# 2026-07-24 会话：WSL 优化 + 构建脚本 + 重建尝试

## 主要完成

### WSL 优化
- 修复 `/etc/wsl.conf`：移除旧版 `[wsl2]` 区段（memory/processors/localhostForwarding），保留 `[interop]` + `[user]`
- 检查 WSL 资源：14核 i9-13900H / 23G 内存 / 920G 可用磁盘 ✅
- Bazel 缓存：`output_user_root` 原属 root → 修复为 axymorrsen

### 构建脚本
- 创建 `scripts/wsl_build_oki.sh` — 一键构建脚本
  - 支持三种模式：`clean`（完整重建）、`full`（增量）、`image`（仅打包）
  - 自动处理 abogki tag、bazel 权限、输出目录
  - 包含 WSL 性能优化（swappiness、dirty_ratio）
  - 自动打包 AK3（含 vendor_boot.img）
  - 已验证：工具检查、路径检查、错误处理

### 重建尝试（未成功）
- `oplus_build_kernel.sh sun perf` 重建多次失败
- 根因：Bazel 增量缓存不完整（之前误删 `out/msm-kernel-sun-perf/`）
- 具体错误链：
  1. `output_user_root` 权限被拒 → 修复
  2. `gki_system_dlkm_modules` 源码缺失 → msm-kernel 浅同步不含此文件
  3. `prepare_vendor.sh` cp 命令找不到前一次构建产物
  4. `mktemp` 目录缺失 → 手动创建后解决
  5. Bazel 直接构建超时 10 分钟无输出
- 结论：从零重建 OKI 需要完整 repo sync（不带 `--no-tags`）+ 完整 bazel 缓存

## 2026-07-24 重建结果
- 修复了 `wsl_build_oki.sh` 中 for 循环 cd 不返回的 bug
- 构建成功！产物: `v6.6.118-OnePlus13-sun-AK3-OKI-20260724_0028.zip`
- ⚠️ 但 abogki tag **未出现在版本串中**：`6.6.118-android15-8-o-ge1b346b6b4f4-4k`
- 原因：标准 `setlocalversion` 不识别 `abogki*` tag，这是 OnePlus 内部 CI 机制

## abogki tag 总结
- 已在 `common` + `msm-kernel` 本地创建 `abogki500782043`（指向 HEAD）
- 远程 OnePlusOSS 无此 tag
- **本地 tag 无效** — OnePlus 构建系统有额外的 tag 处理逻辑未开源
- 替代方案：用 `CONFIG_LOCALVERSION` 或 `localversion-abogki` 文件注入 `-abogki500782043`

## 下一步
- 方案 A: 用现有 `Image` + `vendor_boot.img` 打包 AK3 直接刷（推荐，立即可用）
- 方案 B: 用 `scripts/wsl_build_oki.sh` 跑 `bash scripts/wsl_build_oki.sh image` 打包现有产物
- 方案 C: 修复构建系统后跑 `bash scripts/wsl_build_oki.sh clean` 完整重建
